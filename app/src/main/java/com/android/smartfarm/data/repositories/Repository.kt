package com.android.smartfarm.data.repositories

import okhttp3.OkHttpClient
import javax.inject.Inject

class Repository @Inject constructor(private val okHttpClient: OkHttpClient) {//서버에서 정보 받아오는 함수

}